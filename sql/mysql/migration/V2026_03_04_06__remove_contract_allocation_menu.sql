-- ============================================================
-- 物理删除合同收入分配菜单（合同收入分配功能已迁移至项目管理流程）
-- 菜单 ID 已通过查询确认：5147（页面）+ 5148~5151（按钮权限）
-- ============================================================

-- 1. 删除角色-菜单关联
DELETE FROM system_role_menu WHERE menu_id IN (5147, 5148, 5149, 5150, 5151);

-- 2. 删除菜单本身及 4 个按钮权限
DELETE FROM system_menu WHERE id IN (5147, 5148, 5149, 5150, 5151);
